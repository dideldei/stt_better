[project]
name = "stt-diktat-agent"
version = "0.1.0"
description = "Lokaler STT-Diktat-Agent für eine Hausarztpraxis"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
